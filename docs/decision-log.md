# Decision Log

> Single source of truth for design rationale on the Haze (`libhaze`) initiative,
> maintained per **Rule 1 (Explainability)** of the Agent Action Plan.

## How this log works

This document records the rationale behind every non-trivial implementation
decision on the Haze initiative. Per Rule 1, rationale lives **here and only
here** — it is deliberately kept out of code comments so that code stays
declarative while the reasoning, alternatives, and risks live in one auditable
place.

- **Scope.** Every non-trivial decision, and every deviation from a literal
  reading of the requirements, has a row in the [decision table](#2-decision-table).
  Unexplained deviations are treated as defects.
- **Traceability.** Because this initiative includes migrations and refactors
  (graph capture on top of the epoch subsystem; README examples mirrored into
  `examples/`), the log also carries a bidirectional
  [traceability matrix](#3-traceability-matrix) with 100% coverage.
- **Organization.** The log is append-oriented and organized by work item /
  priority tier (`P1a`, `P1b`, `P2a` … and Rules 1–3). Each pull request cites
  the decision IDs it implements in its description.
- **Stable IDs.** Decision IDs (`D-01`, `D-02`, …) never change or get
  renumbered; new decisions take the next free ID.

### Referenced paths

The decisions and matrices below reference these files. Paths are relative to
this document (`docs/decision-log.md`).

- Grounding sources: [`../README.md`](../README.md), [`../CMakeLists.txt`](../CMakeLists.txt), [`../Makefile`](../Makefile), [`../flake.nix`](../flake.nix), [`../src/core/epoch.hpp`](../src/core/epoch.hpp).
- Companion documentation (this initiative): [`./index.md`](./index.md), [`./architecture.md`](./architecture.md), [`./building.md`](./building.md), [`./testing.md`](./testing.md), [`./observability/README.md`](./observability/README.md), [`./presentation/executive-summary.html`](./presentation/executive-summary.html).
- Rationale pointers elsewhere in the tree resolve back here: [`../CONTRIBUTING.md`](../CONTRIBUTING.md), [`../README.md`](../README.md), [`../CMakeLists.txt`](../CMakeLists.txt), [`../Makefile`](../Makefile), [`../flake.nix`](../flake.nix).

## 2. Decision table

Columns follow the Rule 1 contract: what was decided, what alternatives existed,
why the choice was made, and what risks it carries (with mitigations). The final
column ties each decision to its work item or rule.

| ID | Decision | Alternatives considered | Rationale | Risks / Mitigations | Work item(s) |
|----|----------|-------------------------|-----------|---------------------|--------------|
| <a id="d-01"></a>D-01 | Use **Google Benchmark** for the microbenchmark harness. | Catch2's built-in benchmarking; nanobench; hand-rolled `std::chrono` timing loops. | Industry-standard `for (auto _ : state)` loop with `benchmark::DoNotOptimize()` to defeat dead-code elimination; native JSON output feeds the CI regression gate against a checked-in baseline; available from the pinned nixpkgs as `gbenchmark` (1.9.5); builds as a separate executable linking `haze_objs`, never absorbed into `libhaze`. | Risk: adds a developer-only build dependency. Mitigation: confined to the dev shell and a standalone benchmark executable, so the shipped `libhaze`, its ABI, and the symbol-leak audit are unaffected. | P1b |
| <a id="d-02"></a>D-02 | Measure coverage with **Clang source-based coverage** (`llvm-cov` + `llvm-profdata`) rather than gcov + lcov-gcc. | GCC `gcov` with `lcov`/`genhtml`; a hosted third-party coverage service. | The project standardizes on Clang 19 (the supported floor), so `llvm-cov`/`llvm-profdata` already ship with the toolchain and add no new runtime dependency; `-fprofile-instr-generate -fcoverage-mapping` plus `llvm-cov export -format=lcov` scopes cleanly to `src/core/` and `src/api/` for the 80% target. | Risk: a Clang-only workflow. Mitigation: Clang 19 is the supported compiler floor and the instrumentation is gated behind a Clang-guarded `HAZE_COVERAGE` option, so ordinary builds are unaffected. | P2c |
| <a id="d-03"></a>D-03 | Implement graph capture by **snapshotting the recorded epoch trace** into a new `src/core/graph.{hpp,cpp}` module, not by inventing a new IR. | A dedicated graph IR; overloading `EpochState` to retain multiple replays; leaving the stubs at `HAZE_ERROR_NOT_SUPPORTED`. | `EpochState` records an op sequence once and resets after a single `replay_and_populate()` (state is dropped via `clear_state_locked()` at materialize time), so it cannot express record-once/replay-many alone; cloning the recorded FHETCH op-sequence and input bindings into `GraphState`/`GraphExec` yields deterministic replay with `DevAddr` operand stability, matching CUDA stream-capture / instantiate / launch semantics without a new IR. | Risk: snapshot lifetime and device-memory aliasing across replays. Mitigation: the snapshot must neither leak nor alias device memory between replays; the pointer-stability invariant is exercised by `test/test_graph_capture.cpp`. | P1a |
| <a id="d-04"></a>D-04 | **Reinterpret the Observability rule for a C++ shared library with a C ABI** (explicit deviation from the network-service literal reading). | A literal HTTP metrics endpoint and network tracing (not applicable to a library); or skipping observability (non-compliant). | Haze ships no network surface, so: structured logging with correlation IDs lives in `src/common/log.*` keyed by epoch/stream; the "metrics endpoint" is the `hazeGetPerformanceCounters` query surface; "distributed tracing across service boundaries" becomes op/epoch span tracing across the record -> flush -> replay path and the `replay_bridge/` / OpenFHE boundary; health/readiness maps to lifecycle and config-state introspection; the dashboard template ships under `docs/observability/`. The three documented no-ops stay pure and uninstrumented. | Risk: the reinterpretation could be read as non-compliance. Mitigation: recorded as an explicit, justified deviation (see [Deviations](#41-deviations)) with a one-to-one mapping from each literal requirement to its library-context equivalent. | Rule 2 |
| <a id="d-05"></a>D-05 | Keep the **README example markers authoritative** while additionally shipping byte-consistent mirrors under `examples/`. | Move the canonical examples into `examples/` and have the README transclude them; maintain two independently edited copies. | `scripts/test_readme_examples.sh` extracts and compiles the fenced blocks from `README.md` by HTML markers; keeping the README authoritative preserves the docs-as-tests guarantee unchanged, while `examples/quickstart.c` and `examples/ckks22.cpp` give users standalone files. README-as-source-of-truth with mirrored examples. | Risk: drift between the README and `examples/`. Mitigation: the docs-as-tests CI check keeps the README blocks compiling and running; the sync strategy (mirror from the README, do not hand-edit) is recorded here. | P3b |
| <a id="d-06"></a>D-06 | **Sequence the 80% coverage gate after** the P2d backfill and P2e hardening. | Enable the gate immediately; never gate. | A logical (not schedule) dependency — establish a baseline, raise coverage, then enable the gate — so the branch is never gated red before the coverage exists to clear it on `src/core/` + `src/api/`. | Risk: none material. Mitigation: the gate is enabled only once measured coverage clears 80% on the two scoped trees. | P2c (after P2d/P2e) |
| <a id="d-07"></a>D-07 | Expose performance counters via an **additive `hazePerformanceCounters` struct** populated through the retained `hazeGetPerformanceCounters(void*)` signature. | Add new per-counter getter symbols; return counters through several primitive out-parameters; change the existing signature. | The ABI signature is retained, so adding a struct is purely additive and introduces no new exported symbol; the struct is appended after the existing types with no reordering, preserving ABI stability. | Risk: future counter additions could tempt a layout change. Mitigation: treat the struct as append-only — never reorder or resize existing fields. | P2b |
| <a id="d-08"></a>D-08 | Build the **benchmark harness and coverage build as separate executables/configurations**, never absorbed into the shipped `libhaze`. | Compile benchmarks or coverage instrumentation into the library or its default build. | Keeps the shipped `libhaze` byte-identical (apart from the additive struct) and keeps the symbol-leak audit green by construction; internal C++ symbols mangle to `_ZN4haze…` and are localized by the existing version script. | Risk: two extra build configurations to maintain. Mitigation: both are gated behind opt-in CMake options (`HAZE_BUILD_BENCHMARKS`, `HAZE_COVERAGE`) and are exercised only in their dedicated CI jobs. | P1b, P2c |
| <a id="d-09"></a>D-09 | Implement peer access for the **simulator-representable portions only**, flagging physical multi-chip steps as human follow-up. | Implement full multi-chip peer access now; leave the stubs at `HAZE_ERROR_NOT_SUPPORTED`. | The record-and-replay simulator can model peer topology and same-process peer copies, but physical multi-chip transfer needs hardware absent from CI; implementing the representable portion advances the API while being honest about the hardware boundary. | Risk: hardware-only paths cannot be validated in CI. Mitigation: hardware-only assertions in `test/test_peer_access.cpp` sit behind a dedicated tag so the default suite stays green; the human follow-up is called out in the PR description. | P2a |
| <a id="d-10"></a>D-10 | Add new CI jobs as **new workflow files** rather than editing the seven existing workflows. **Delivered:** `.github/workflows/benchmark.yml`, `.github/workflows/coverage.yml`, and `.github/workflows/sanitizers.yml` are all present in the tree. | Extend `build-test.yml` with additional jobs. | Isolating new jobs keeps the seven established workflows intact and green, simplifies review, and lets each job be enabled or made required independently. Each is a small single-purpose workflow: the benchmark job runs the harness and compares against the checked-in baseline (`make bench` — see [D-30](#d-30)); the coverage job builds with the `HAZE_COVERAGE` option and runs the `make coverage` / `scripts/coverage.sh` flow against the 80% gate; and the sanitizer job exercises the `HAZE_SANITIZERS` (ASan/UBSan) and `HAZE_TSAN` build options. | Risk: more workflow files to track. Mitigation: each file is small and single-purpose, named for the work item it serves. | P1b, P2c, P2e |
| <a id="d-11"></a>D-11 | Leave `linker/haze_exports.map` and `linker/haze_exports.sym` **unchanged**. | Add explicit entries for the newly-filled functions. | The graph, peer, and counter functions are already exported stubs; the version script matches the `haze*` C ABI by wildcard, so filling the stubs adds no new public symbol and needs no script change — the symbol-leak audit stays green by construction. | Risk: a future non-`haze*` export would slip the wildcard. Mitigation: `scripts/check_symbol_leak.sh` fails on any exported symbol outside `haze*`. | P1a, P2a, P2b |
| <a id="d-12"></a>D-12 | **Embed the Blitzy reveal.js theme inline** in `docs/presentation/executive-summary.html`. | Reference the canonical `blitzy-deck/references/blitzy-reveal-theme.css` by path; fabricate a theme. | The canonical theme file is not present in the checkout and the deck must be a single self-contained HTML file, so the theme is embedded inline; the canonical file is treated as an external reference and its contents are not fabricated. | Risk: the inline theme could drift from the canonical one. Mitigation: the inline copy is clearly marked as embedded; if the canonical file is later added to the checkout, the deck can be re-synced from it. | Rule 3 |
| <a id="d-13"></a>D-13 | **Decouple reveal.js initialization from the Mermaid CDN load** so the deck comes up styled even when the diagram CDN is unreachable, and correct the earlier "opens offline" claim. | Keep the static top-level `import mermaid …` (any CDN miss aborts the whole module and reveal never initializes, leaving an unstyled vertical dump); bundle/inline Mermaid to make the deck truly offline (breaks the Rule 3 pinned-CDN mandate). | reveal.js and Lucide load via classic `<script>` tags and initialize first; Mermaid is then loaded with a dynamic `import()` inside `try/catch` after reveal init, so a diagram-CDN failure is confined and degrades to per-node captioned fallbacks instead of blanking the deck. The pinned Mermaid 11.4.0 URL is retained, honoring Rule 3's pinned-CDN mandate; the inaccurate "opens offline" wording is corrected to "requires network for pinned CDN assets; degrades gracefully offline". | Risk: diagrams are unavailable with no network. Mitigation: each `.mermaid` node is replaced with a styled fallback caption and stays hidden until processed, so no raw source flashes and no slide is blank; the deck remains fully navigable. | Rule 3 |
| <a id="d-14"></a>D-14 | Render the slide-10 gauge center label and the slide-11 progress labels as **HTML (via `<foreignObject>` / flanking `<div>`s) rather than SVG `<text>`**. | Keep SVG `<text>` with `dominant-baseline`; only convert the unitless font-size to `px`. | SVG `<text>` mis-paints under reveal.js fractional transform scaling when the text is gradient-filled or sits in an extreme-aspect viewBox — the glyphs rasterize near their unscaled size and drift or clip (observed at narrow breakpoints). HTML inside `<foreignObject>` scales with the SVG viewBox exactly like the vector shapes, and a gradient applied via `background-clip:text` rasterizes correctly, so the label stays centered and crisp at every breakpoint. | Risk: `<foreignObject>` / `background-clip:text` are comparatively less common. Mitigation: both are well-supported in evergreen browsers; the result was verified by screenshot across 1920/1366/768/390 widths, since `getBoundingClientRect` reported correct geometry even while the raster was wrong. | Rule 3 |
| <a id="d-15"></a>D-15 | Force reveal.js **scroll view onto the dark brand theme and disable its narrow-width auto-activation** (`scrollActivationWidth: null`). | Leave reveal.js defaults (scroll view auto-activates below ~435px with a white background); restyle only after detecting scroll view at runtime. | On a fresh narrow load reveal.js can open in scroll view, whose layers default to white in `reveal.css` and render the light brand text near-invisible (a contrast failure). Setting `scrollActivationWidth: null` keeps the deck in letterboxed slide view at every width, and dark-theme overrides on `.reveal-viewport` / `.scroll-page` / `.scroll-page-content` guarantee the brand background and legible text if scroll view is ever entered. | Risk: an additive config key plus a few `!important` overrides. Mitigation: the key is purely additive and the mandated config keys are untouched; contrast was re-verified via a Lighthouse audit with color-contrast passing. | Rule 3 |
| <a id="d-16"></a>D-16 | **Emit each `log_error` diagnostic as a single composed line written with one `std::fwrite` to `stderr`** (line-atomic), refining the correlation-ID logging recorded under [D-04](#d-04). | Perform several independent writes/insertions per record (one per field); serialize every emission behind a process-global `std::mutex`; use `std::osyncstream`; switch to `std::print` / `std::format`. | Composing the whole `[haze] [cid=<id>] <tag>: <body>\n` line into one `std::string` and emitting it with a single `std::fwrite(record.data(), 1, record.size(), stderr)` maps to one underlying stdio write, so records do not interleave under concurrent multi-threaded logging — satisfying the structured/parseable-diagnostics goal of Rule 2 and the checkpoint's "line integrity, concurrency isolation" property. Line integrity is additionally guaranteed deterministically by `append_escaped`, which replaces every newline, C0/C1 control, DEL, and non-ASCII byte in the tag and body with a printable `\xNN` (or `\\`) escape, so no field can inject a framing break; this escaping invariant is what the executable evidence in `test/test_documented_noops.cpp` verifies rather than a timing-dependent thread race. A global mutex adds lock-ordering surface and contention to a `noexcept` sink; `std::osyncstream` / `std::print` / `std::format` add include and allocation churn — so the single-`fwrite` approach fixes the defect with the smallest blast radius. | Risk: composing a `std::string` allocates, and an allocation failure inside a `noexcept` function would call `std::terminate`. Mitigation: the composition and single-shot write sit inside a `try/catch (...)` whose handler emits a fixed, allocation-free generic notice via `std::fputs("[haze] log sink dropped a record (formatting or write failure)\n", stderr)` — a dropped-record notice, not a re-emission of the formatted record — so `noexcept` holds and no exception can cross the C ABI boundary. The added `<string>` include is a standard-library header only; no new symbol is exported, so the symbol-leak audit is unaffected. | Rule 2 (refines [D-04](#d-04)) |
| <a id="d-17"></a>D-17 | **Deliver `test/test_allocator_limits.cpp`** as part of the integrated branch, superseding the earlier deferral. | Leave it deferred to a later PR; fold its cases into `test/test_error_paths.cpp`. | With the single-pass integrated delivery (see [D-22](#d-22)) there is no separate later PR to defer to, so the allocator pool-exhaustion / zero / oversized boundary TU is created, registered exactly once in `add_executable(haze_tests ...)`, and runs in the default suite. The earlier "deferred / not in the tree" rationale is withdrawn because it is no longer true against the checkout. | Risk: none material. Mitigation: the file is registered and executed alongside the rest of the P2e hardening suite. | P2e |
| <a id="d-18"></a>D-18 | Deliver the **epoch/allocator lock-order concurrency test inside `test/test_error_paths.cpp` behind the hidden `[.][concurrency]` tag**, and scope it to independent per-thread allocation churn. | Put it in the deferred `test_allocator_limits.cpp`; drive concurrent op-recording through `compute`/`flush`; omit a lock-order test entirely (the R3 gap). | A concurrency test addressing the epoch->allocator lock-order surface (CWE-667/833) is delivered now so the hardening categories are not left without one. It uses a `std::barrier` to release eight threads simultaneously, each performing malloc->memset->free on thread-owned pointers, maximizing allocator-lock contention under TSan without violating the single-writer record-and-replay invariant. Deeper *nested* epoch->allocator ordering through concurrent op recording is deferred, because record-and-replay is single-writer by design and cannot record ops from multiple threads concurrently. The case sits behind `[.]` so it is opt-in (run under a local `HAZE_TSAN=ON` sanitizer build) and does not perturb the default suite. | Risk: the test cannot exercise concurrent op recording. Mitigation: the single-writer limitation is documented here as a deviation from a literal "epoch/allocator lock-order under concurrency" reading; the delivered test still exercises the allocator lock under contention. | P2e |
| <a id="d-19"></a>D-19 | **Smoke-test the three documented no-ops** (`hazeStreamSynchronize`, `hazeStreamWaitEvent`, `hazeDeviceSynchronize`) for `HAZE_SUCCESS` and, in addition, assert their **exclusion from telemetry** with executable evidence. | Assert only the return code; instrument the no-ops (prohibited by AAP §0.3.2). | The AAP forbids changing no-op behavior, so the tests assert `HAZE_SUCCESS` without modifying them; to satisfy the Observability requirement that the no-ops stay *uninstrumented*, `test/test_documented_noops.cpp` snapshots all seven performance counters and the thread-local correlation ID before and after the three calls and asserts every value is unchanged — turning "not instrumented" into a checked invariant rather than a claim. | Risk: none — the functions are unchanged. Mitigation: the evidence is read-only observation of the public counter surface and the correlation getter. | P2d, Rule 2 |
| <a id="d-20"></a>D-20 | Prove **no C++ exception crosses the `noexcept` C ABI boundary using a fork-subprocess death-test** in `test/test_error_semantics.cpp`. | `REQUIRE_NOTHROW` around the ABI calls (cannot observe a `noexcept` violation — the process has already `std::terminate`d); add a throw-injection seam to the sink. | `errors.hpp` exposes no throw-injection seam, and a throw escaping a `noexcept` function calls `std::terminate` before any in-process matcher can react, so `REQUIRE_NOTHROW` gives false assurance (the ES1 defect). The child process drives eight pathological `noexcept`-ABI calls with pinned error codes and `_exit(rc)`; the parent asserts `WIFEXITED` and `WEXITSTATUS==0`, so a `std::terminate` would surface as an abnormal child exit and fail the test deterministically. | Risk: fork-based tests are POSIX-only. Mitigation: the supported platforms are all POSIX (x86_64/aarch64 Linux, aarch64 Darwin); the wait-status macros are annotated with the repo-standard `// NOLINTNEXTLINE(misc-include-cleaner)` precedent. | P2e |
| <a id="d-21"></a>D-21 | Write `test/test_config_errors.cpp` against the **actual `Config::set_modulus` sparse-write/contiguity + post-configure-immutability contract**, deviating from the review's suggested "assert index 64 is rejected after populating 0–63". | Implement the reviewer's literal suggestion (assert a hard 64-index cap). | An empirical probe proved `Config::set_modulus` enforces only `idx>=0`, `modulus!=0`, contiguity (`idx>size` gap -> `INVALID_VALUE`; `idx==size` append; `idx<size` overwrite), and post-`configureDevice` immutability (a differing value or new index -> `NotConfigured`/`CONFIGERR`); it has **no** hard 64-index cap. Filling slots 0–63 and then writing index 64 (and 65) *succeeds*. The `kMaxCiphertextModuli=64` limit applies to MRP *allocation count*, not to `set_modulus`. Asserting a nonexistent cap would test a false contract, so the test exercises the real one. | Risk: the test diverges from the review's literal wording. Mitigation: this deviation is logged per Rule 1 with the probe evidence; the real contiguity/immutability contract is exercised across eleven cases, which is stronger than the single-bound assertion originally suggested. | P2e |
| <a id="d-22"></a>D-22 | **Deliver the whole initiative as one integrated branch** (runtime + tests + benchmarks + observability together), superseding the earlier per-PR "test-specification milestone" framing that treated the runtime as deferred. | Keep the test-specification-only framing and land the runtime in later PRs; split the remediation back into per-work-item PRs. | The remediation resolved every review finding — including the graph/peer/counter *runtime* — in a single coherent branch, so `src/core/graph.*`, `src/core/metrics.*`, the benchmark harness, and the observability wiring are all present and pass their suites now. Describing them as "deferred targets" would be false against the tree, which is exactly the staleness Rule 1 forbids; the log is therefore re-grounded to "delivered and verified," and only genuinely-absent surfaces (physical multi-chip peer-access validation) remain marked deferred. | Risk: a reader expecting per-PR granularity. Mitigation: the forward/backward matrices now state delivered-vs-deferred per artifact against the actual tree. | Rule 1 (supersedes [D-17](#d-17)) |
| <a id="d-23"></a>D-23 | Give peer access a **single coherent single-device authorization contract**: a device is never its own peer; a same-device copy is always authorized; a distinct-device copy requires a prior successful `hazeDeviceEnablePeerAccess`; `hazeDeviceReset` returns peer state to the single-device default. | Keep the superseded state where `canAccessPeer(0,0)` was false yet a same-device peer copy still succeeded (the P1 contradiction); treat every device as its own peer. | The prior model was internally contradictory (capability query disagreed with copy authorization) and let unauthorized distinct-device copies through. The coherent contract makes `hazeDeviceCanAccessPeer`, `hazeDeviceEnablePeerAccess`, and the copy-authorization check in `hazeMemcpyPeerAsync` agree, and is exercised by `test/test_peer_access.cpp`. | Risk: single-device simulator cannot exercise the enabled distinct-peer success path. Mitigation: that path sits behind the hidden `[.][hardware]` tag; physical multi-chip transfer is human follow-up (see [D-09](#d-09)). | P1 (refines [D-09](#d-09)) |
| <a id="d-24"></a>D-24 | Clone the recorded program directory for graph capture through a **hardened `secure_clone_project_dir`**: `mkdtemp` with `0700`, copy **without** overwriting existing files, `chmod` the tree owner-only, and a scope-guard that removes a partial clone on any failure. | Keep the superseded predictable-name / overwriting / world-readable copy with best-effort cleanup. | Graph snapshots contain FHE material; the earlier predictable-name, non-`0700`, overwriting copy risked a world-readable leak and left partial state on failure (the G4 defect). The hardened helper eliminates the predictable path, the overwrite, and the permissive mode, and guarantees cleanup on the error path. | Risk: `mkdtemp`/`chmod`/`copy` can fail. Mitigation: all failures translate to a `HazeInternalError` inside the `noexcept` boundary with the partial directory removed (see [D-25](#d-25) for the noexcept discipline). | G4 |
| <a id="d-25"></a>D-25 | Define an **explicit counter-accounting and consistency policy**: `op_count` increments **once per high-level op** regardless of SRP/MRP residue fan-out and **never** counts device-to-device copies (those advance `bytes_d2d` only); every counter is a plain `uint64_t` guarded by one leaf mutex with **saturating** adds and an atomic (no-torn-read) snapshot/reset. | Keep the superseded per-residue `op_count` increment and unmetered MRP D2D; keep relaxed atomics that permit torn snapshots and silent wrap. | The per-residue increment overstated op counts for MRP and left MRP D2D bytes uncounted (M1); relaxed atomics allowed torn snapshots and silent wraparound (M3). Metering at the high-level op site (compute/basis-convert), charging D2D bytes after a copy actually succeeds, and serialising all counter access under the leaf mutex with saturating adds gives a well-defined, testable contract (`test/test_performance_counters.cpp`). | Risk: saturation is a terminal state for a counter until reset. Mitigation: documented as intentional; wrap would be worse (a false regression), and 64-bit saturation is unreachable in practice. | M1, M3 (refines [D-07](#d-07)) |
| <a id="d-26"></a>D-26 | **Keep the `src/core/{graph,metrics}.cpp` and new-test registrations in `CMakeLists.txt`** (each source registered exactly once), declining the review's suggestion to revert them to a separate "final integration" change. | Revert the registrations from this change and land them only in a designated final-integration pair, as the C1 finding literally suggested. | AAP §0.6.1 lists `CMakeLists.txt` as an in-scope UPDATE that must "register all new test files," and §0.6.5 states every new `test/*.cpp` "must be added to the `add_executable(haze_tests ...)` list or it will not be compiled or run." The initiative is a single integrated deliverable (see [D-22](#d-22)), so reverting the registrations would leave `graph.cpp`/`metrics.cpp` out of `haze_objs` and fourteen test TUs uncompiled — breaking the build and contradicting the AAP. Per the AAP-precedence rule, the AAP wins over a finding's suggested resolution; the actionable half of C1 (exactly-once registration) is satisfied and verified. | Risk: reads as leaving an out-of-scope file modified. Mitigation: the file is explicitly in AAP scope; each source is registered exactly once (verified), and this decision cites the mandating AAP sections. | C1 |
| <a id="d-27"></a>D-27 | Gate span tracing on a **re-read-each-call `HAZE_TRACE` environment check** (not a cached one-shot read), and correct the `trace_enabled()` doc comment to match. | Cache the gate on first use like the `HAZE_DEBUG` gate; leave the stale "evaluated once on first use" comment. | Re-reading `HAZE_TRACE` on every call lets tracing be toggled at runtime and, crucially, lets a test enable it in-process, drive a real `hazeFlush`, and capture the emitted `epoch.flush` begin/end spans — the end-to-end observability evidence O1/T4 require. The header comment previously claimed a cached one-shot read, which contradicted the implementation and misled test authors, so it was corrected. | Risk: a per-call `getenv` on the trace path. Mitigation: tracing is off by default (the gate short-circuits) and the check is a single `getenv` only when a span is emitted. | O1, T4 |
| <a id="d-28"></a>D-28 | **Redact on-disk paths in the log sink to their basename** before emission. | Log full absolute paths; drop path fields entirely. | Correlation/diagnostic lines reference the cloned program directory and other on-disk paths; emitting full paths would leak host filesystem layout (and potentially temp-dir names holding FHE material), while dropping them removes useful context. Keeping only the basename preserves diagnostic value without disclosing the containing path, and is asserted by executable evidence in `test/test_documented_noops.cpp`. | Risk: basename alone can be ambiguous across directories. Mitigation: correlation IDs disambiguate related records; the redaction is a deliberate confidentiality/utility trade-off. | O1 |
| <a id="d-29"></a>D-29 | Expose readiness as a **`runtime_readiness()` query composed in the epoch layer** that reads config-configured, backend-initialized, and epoch-recording state, each under its own lock with no nesting. | Add a new public ABI readiness symbol; nest the three locks under one; scatter the checks across call sites. | Health/readiness is the library-context mapping of the Observability rule (see [D-04](#d-04)); composing it in the epoch layer reuses the existing singletons without a new exported symbol (preserving the ABI and the symbol-leak audit) and avoids introducing a new lock-ordering edge by taking each leaf lock independently. | Risk: a readiness read is a point-in-time snapshot across three locks. Mitigation: readiness is advisory introspection, not a transactional guarantee; each sub-state is itself consistently locked. | O1 |
| <a id="d-30"></a>D-30 | Link the benchmark executable against the **installed `libhaze` shared object** and check in a **genuinely-measured `baseline.json`** with host/build metadata; treat the CI regression check as advisory on shared runners. | Link `haze_objs` directly (as the harness sketch first did); keep the 1970 placeholder baseline with inflated timings that could never fail the gate. | Linking the shipped `libhaze` benchmarks exactly what users run and keeps the harness a pure consumer of the C ABI. The prior `baseline.json` was a placeholder (epoch 1970, huge timings) that made the gate vacuous (the B1 defect); replacing it with real measurements plus environment metadata makes regressions detectable. Because CI shares runners with variable performance, the comparison is scoped as advisory with a tolerance rather than a hard fail on noise. | Risk: shared-runner variance causes noisy comparisons. Mitigation: a tolerance band and recorded host metadata; the baseline is refreshed when the environment changes. | P1b, B1 |
| <a id="d-31"></a>D-31 | Keep `examples/` **outside the `clang-format` enforcement scope** (`scripts/clang-format.sh` covers `src include replay_bridge test` only) while keeping the example sources README-authoritative with checked mirrors and checked API calls. | Add `examples/` to the format-enforced scope; hand-maintain the examples independently of the README. | The examples are generated mirrors of the authoritative README fenced blocks (see [D-05](#d-05)); enforcing repository `clang-format` on them would fight the README's presentation formatting and the docs-as-tests extraction. They are instead kept format-idempotent by construction and every Haze/bridge call is result-checked with cleanup (the E1 fix), with `scripts/test_readme_examples.sh` proving README↔`examples/` parity portably (the E2 fix). | Risk: examples could drift from repo style. Mitigation: they mirror the README verbatim and compile/run in CI; the format scope is documented here rather than in code. | E1, E2, Q2 (refines [D-05](#d-05)) |
| <a id="d-32"></a>D-32 | **Place each concurrency test in the file that owns the surface it stresses**, and allow one counter-concurrency case to reach the internal aggregator directly. | Concentrate every concurrency case in `test/test_error_paths.cpp`; test only through the public API everywhere. | Peer-call races belong with the peer suite (`test/test_peer_access.cpp`), graph launch/update/destroy lifetime races with the error-path suite (`test/test_error_paths.cpp`), and counter snapshot/reset races with the counter suite (`test/test_performance_counters.cpp`) — co-locating each with its subject keeps the stress test next to the contract it defends. The counter suite is otherwise strictly public-query-only, but snapshot-vs-`reset()` under live mutation can only be raced safely at the aggregator level (a public `hazeDeviceReset` tears down the allocator and cannot race active device work), so one `[.][concurrency]` case reaches `haze::metrics()` directly, documented in that file's header. All cases are hidden by `[.]` and were run under `HAZE_TSAN=ON` with zero data races. | Risk: reaching an internal symbol departs from the file's public-only philosophy. Mitigation: it is the single documented exception, isolated to one hidden case, and justified by the tear-down hazard of a public reset race. | T1, T2, T3, T4 |
 | <a id="d-33"></a>D-33 | Render every Mermaid diagram in the executive deck under the **official `securityLevel: 'sandbox'`** mode (each diagram drawn in a sandboxed `<iframe>` with no `allow-scripts`), keep all diagram definitions **fixed and trusted** (static text authored in the deck, never user-supplied), and add **two runtime regression guards**: (1) refuse any non-sandbox/-strict security level, and (2) reject any static definition carrying an interactive `click`/`javascript:`/`<script>` directive, dropping it to a caption. | Upgrade the Mermaid pin past 11.4.0; use `securityLevel: 'strict'`; route the rendered SVG through DOMPurify before `innerHTML`. | Rule 3 freezes the CDN asset versions (Mermaid 11.4.0), so upgrading the pin is not permitted here; under that frozen pin the upstream advisory (GHSA-ghcm-xqfw-q4vr) names sandbox mode as the mitigation for Mermaid's HTML/DOM-injection class — the sandboxed iframe carries no `allow-scripts`, so nothing a diagram emits can execute in the deck context, which neutralises both the prior `securityLevel: 'loose'` setting and the `innerHTML` SVG injection (the injected markup is now a self-isolating iframe). `'strict'` is also safe but disables the `htmlLabels` the deck's flowcharts rely on, degrading the already-verified visual rendering; DOMPurify would add an unpinned third-party dependency, contradicting the self-contained-deck constraint. The definitions are static and non-interactive, so losing sandbox-disabled interactivity (clicks/links) costs nothing. | Risk: sandbox renders into an iframe whose background/sizing differ from an inline `<svg>`, and interactivity is disabled. Mitigation: dedicated `.mermaid iframe` CSS caps size to the 1080p canvas; the deck's diagrams are static (no clicks/links), and the two guards fail closed to the existing caption fallback. | S1 (refines [D-13](#d-13)) |
| <a id="d-34"></a>D-34 | On `hazeGraphLaunch`, **re-dispatch the snapshotted trace in-process against the current device shadows** rather than re-applying output values cached at capture time; the snapshot stores the recorded op-sequence plus the input `DevAddr` bindings and a trace signature, never output values. | Cache the output values computed once at `end_capture_snapshot` and re-apply them on every launch (the shipped defect); re-record the whole epoch on each launch (defeats record-once / replay-many); require identical inputs on every launch (breaks the CUDA-like semantics D-03 targets). | D-03 promises deterministic replay with `DevAddr` operand stability matching CUDA stream-capture / instantiate / launch, where a launch re-runs the recorded work against the same addresses using whatever data currently lives there. Caching outputs made launch a no-op that ignored input mutations, directly contradicting D-03 (the CRITICAL Issue 1 the QA review flagged as undocumented). Snapshotting the input bindings and, on each launch, reading the current shadow contents at those addresses via `read_polynomial_components` before re-running the trace makes an input overwritten at a stable address correctly change the replayed result. | Risk: reading stale or uninitialized shadows if a bound address was freed between launches. Mitigation: the snapshot pins the input bindings and each launch repopulates `captured_inputs` from the current shadows before re-dispatch; `test/test_graph_capture.cpp` exercises overwrite-at-a-stable-address relaunch (33 assertions) and proves recomputation rather than replay of cached values. | P1a (refines [D-03](#d-03)) |
| <a id="d-35"></a>D-35 | Gate `hazeGraphExecUpdate` and reject a same-address different-operation refresh on a **trace-signature comparison computed over register-canonicalized trace lines** — each `%<digits>` operand token is folded to `%r<first-appearance-index>` per file before an FNV-1a hash. | Compare only the output `DevAddr` addresses (the shipped Issue 2 defect — a different operation writing the same address passed the check); hash the raw trace text verbatim (BUG A — over-rejects a legitimate same-topology rebind because operand register ids shift with allocation history); compare opcodes positionally while ignoring operands (too coarse — misses operand-arity or wiring changes). | FHETCH register ids `%N` are assigned by allocation history, not by position, so two structurally identical traces can carry different literal register numbers; hashing the raw text therefore rejects a valid same-topology rebind. Canonicalizing every `%<digits>` to its first-appearance index makes the signature depend on trace structure (opcodes plus operand wiring) instead of incidental numbering, so a genuine op change (add vs mul writing the same address) flips the signature and is rejected with `HAZE_ERROR_INVALID_VALUE`, while a legitimate same-op rebind keeps the signature stable and is accepted. | Risk: a hash collision could admit a genuinely different topology. Mitigation: FNV-1a over the canonicalized line set makes accidental collisions negligible for these small op-sequences, and the update fails closed when either signature is zero or unknown; three tests cover accept-same-op rebind (36 assertions), reject-different-op update (45), and reject-same-address different-op refresh (32). | P1a (refines [D-03](#d-03)) |
| <a id="d-36"></a>D-36 | Wrap the launch-time re-dispatch in an **RAII `CapturedStateGuard` that clears the vendor `Compiler` singleton's captured-output state on every exit path** from `replay_snapshot_locked`. | Rely on `Compiler::start()` to reset captured state (it does not); clear captured state only on the success path (leaks on the error path); clear it eagerly at capture time (too early — the snapshot's own re-dispatch still needs it). | `replay_snapshot_locked` rehydrates the vendor `Compiler` singleton's `captured_outputs` from `outputs.json` and leaves it populated; because `Compiler::start()` does not clear captured state, a launch's output binding leaked into the next capture and corrupted a subsequently captured graph (BUG B). An RAII guard whose destructor clears the vendor captured state guarantees the leak is cleared on both the normal and the exception or error exit, isolating each launch from the next capture. | Risk: clearing vendor state the vendor still needs. Mitigation: the guard fires only after the snapshot's re-dispatch has consumed the captured state; the full graph-capture suite (386 assertions / 22 cases) and the capture-after-launch sequencing tests confirm no cross-contamination and zero regression. | P1a (refines [D-03](#d-03)) |
| <a id="d-37"></a>D-37 | (a) Wrap every `fhetch::result()` read in the `noexcept` epoch functions (`do_materialize_locked`, `replay_snapshot_locked`) in a `try / catch (...)` that maps a failed or partial cereal deserialize to `BackendReplayFailed` -> `HAZE_ERROR_INTERNAL`; (b) **require a per-process `hazeSetProgramDirectory` for concurrent processes**, documented in `include/haze/haze.h`. | Leave the cereal read unguarded (the Issue 3 defect — a `cereal::Exception` escapes the `noexcept` C ABI and calls `std::terminate`); add a global lock around the shared recording directory (does not fix cross-process data mixing and adds contention); serialize concurrent processes (defeats parallelism). | A partial or corrupt on-disk artifact makes cereal throw, and a throw escaping a `noexcept` boundary calls `std::terminate`, crashing the host across the C ABI (the observed intermittent Issue 3 failure). Guarding every `result()` read confines the throw and translates it to the already-documented `HAZE_ERROR_INTERNAL` surface, so a corrupt read fails cleanly and the process survives. The cross-process data-mixing root cause is the shared base recording directory, not the private per-graph/exec snapshot directories (each created under a unique, unpredictable `haze_graph_` mkdtemp path, so their names never collide), so requiring each concurrent process to set a distinct program directory removes the shared-state hazard without any ABI change. | Risk: a guard could mask a legitimate error as `INTERNAL`. Mitigation: the guard fires only on deserialize failure and maps to the documented `INTERNAL` code; two deterministic tests prove it — a corrupt on-disk trace hits the `CompilerBackend::replay` guard (27 assertions) and a corrupt serialized probe, with the template removed so reconstruction skips, hits the `fhetch::result()` cereal guard (28 assertions) — both yielding `HAZE_ERROR_INTERNAL` with the process intact, verified with `HAZE_DEBUG=1` showing the exact guard message. | P1a (refines [D-03](#d-03)) |
| <a id="d-38"></a>D-38 | Harden `scripts/coverage.sh` so it **fails closed and can never report a misleading pass**: (a) propagate a non-zero instrumented-test-suite exit to the script's own exit status even when the profraw still merges and the gate would otherwise pass; (b) reject a non-numeric `COVERAGE_THRESHOLD` up front with `exit 2` before building or running anything; (c) pass `--ignore-errors inconsistent,unsupported` to the optional `genhtml` call and guard it so an HTML-render failure warns and continues instead of aborting the run before the gate. | (a) Keep the prior warn-only test-failure handling (a failing suite still flushes a valid ~85% profraw, so the export and gate succeed and the script exits 0 -- a false PASS); (b) leave the threshold unvalidated (the gate coerces its operands with awk `t + 0`, so a typo such as `8O` silently becomes `0` and disables the gate); (c) leave the `genhtml` call bare under `set -euo pipefail` (lcov 2.x rejects llvm-cov's lcov export with an `(inconsistent) function not hit but line is hit` error and aborts the script before the gate, even though HTML is only an optional convenience). | The coverage gate exists precisely to fail a run whose coverage cannot be trusted; a warn-only test failure, a silently-zeroed threshold, or an abort-before-gate on an optional HTML step each defeat that purpose. Recording the suite exit in `test_failed` and re-asserting it after the gate guarantees a failing suite can never yield a passing coverage verdict; validating the threshold against `^[0-9]+([.][0-9]+)?$` and exiting `2` turns a silent misconfiguration into a hard error; scoping `--ignore-errors inconsistent,unsupported` to the optional HTML render (behind an absence-and-failure guard) keeps the machine-readable lcov export and the gate -- the authoritative surfaces -- always running. | Risk: `--ignore-errors` could mask a genuinely malformed lcov file during the HTML render. Mitigation: the gate reads the merged lcov independently of `genhtml`, so a real data problem still surfaces through the export/gate path; the ignored classes are exactly the benign llvm-cov/lcov-2.x interop warnings, verified by re-running `--html` to rc 0 with a valid `index.html` at 85.29%, and the fail-closed test-exit path is proven by a shim that makes merge+export succeed while the suite exits non-zero, yielding a script exit of 1. | P2c |
| <a id="d-39"></a>D-39 | Force `-DHAZE_BUILD_TESTS=ON` in the `make coverage` target so a coverage build always configures the instrumented `haze_tests`, independent of any stale CMake cache. | Pass only `-DHAZE_COVERAGE=ON` and rely on the `HAZE_BUILD_TESTS` default of ON (the prior behavior); alternatively force the flag in the generic `config`/`build` targets as well. | The coverage pipeline **requires** `haze_tests` (it runs the instrumented binary to emit profraw), so a pre-existing `HAZE_BUILD_TESTS:BOOL=OFF` in the cache (left by an earlier benchmark-only or tests-off configure) makes `make coverage` reconfigure with tests skipped and then fail with no binary to run -- an environment-dependent break. Pinning the flag in the coverage target alone makes that target self-sufficient without overriding the legitimate tests-off opt-out that `config`/`build` honor for benchmark-only builds (documented in `CMakeLists.txt`). | Risk: forcing the flag reconfigures a tests-off build directory when `make coverage` is invoked against it. Mitigation: that reconfigure is the intended and necessary behavior for coverage; a negative-control bare reconfigure (flag absent) was shown to leave the stale `OFF` in place, whereas the fixed target flips the cache to `ON` and regenerates the CTest entries with the instrumented `haze_tests` intact. | P2c |
| <a id="d-40"></a>D-40 | Make each Makefile build-mode target pin **both instrumentation toggles** on every configure — the OFF-direction complement of [D-39](#d-39): the `config`/`build` targets pass `-DHAZE_COVERAGE=OFF -DHAZE_BUILD_BENCHMARKS=OFF`, `make bench` also passes `-DHAZE_COVERAGE=OFF`, and `make coverage` also passes `-DHAZE_BUILD_BENCHMARKS=OFF`, so every target is hermetic regardless of prior cache state. | Keep the prior asymmetric handling (the shipped defect: CMake `option()` keeps a cached `ON`, so a normal `make build`/`make test` after `make coverage` or `make bench` in the same `BUILD_DIR` silently ships the coverage-instrumented library and leaks a `default.profraw`); detect the option delta and force a full reconfigure; add a separate `make reset-options` target; also force `-DHAZE_BUILD_TESTS` in `config`/`build` (rejected — that would override the legitimate tests-off opt-out [D-39](#d-39) deliberately preserves for benchmark-only builds). | CMake `option()` assigns its default only when the variable is not already cached, so an `ON` written by a previous coverage/benchmark configure persists into a later ordinary build in the same directory; the shipped `libhaze` must be byte-for-byte unaffected by benchmark/coverage builds (see [D-08](#d-08), AAP R1/R3). Pinning both instrumentation toggles per target makes each target deterministic and self-describing — the same technique [D-39](#d-39) already applied in the ON direction for `HAZE_BUILD_TESTS` — so an ordinary build after any instrumented build restores the clean baseline library and a normal test run emits zero profraw. Only the two instrumentation toggles are reset in `config`/`build`; `HAZE_BUILD_TESTS` is intentionally left to its opt-out per [D-39](#d-39). | Risk: a plain `make build` now reconfigures a directory a user had deliberately switched to coverage/benchmark mode. Mitigation: that is exactly the intended "return to a clean shipped library" behavior, and coverage/benchmark mode is re-entered by running `make coverage`/`make bench` again. Verified at runtime that a normal-to-bench-to-coverage-to-normal sequence restores the clean baseline library (sha `963190e7…`, size 7,697,728, zero `__llvm` coverage sections) with zero leaked profraw, and that `make bench`/`make coverage` stay functional with hermetic caches. | P2c, P1b (complements [D-39](#d-39)) |
| <a id="d-41"></a>D-41 | Correct the stale Makefile documentation that claimed the default `MODE` is `release` to state the actual default of `debug`, leaving the `MODE ?= debug` assignment (and thus the build-directory selection) unchanged. | Flip the default to `release` to match the docs (rejected — `MODE ?= debug` is a deliberate maintainer choice reverting an earlier release default, so flipping it would undo that decision and change which build directory a bare `make` uses); leave the mismatch unaddressed (rejected — Rule 1 treats an unexplained documentation/behavior mismatch as a defect). | The header comment and `make help` stated `default: release` while `MODE ?= debug` selects `dbuild/`; aligning the documentation to the actual behavior removes the ambiguity with zero behavioral risk and respects the deliberate debug default. All CI workflows and the benchmark/coverage flows pass an explicit `MODE=release`, so the default is never exercised in the merge gates, and `make bench` separately documents that `MODE=release` is recommended for meaningful numbers. | Risk: none material — documentation-only. Mitigation: the `MODE ?= debug` behavior is unchanged and CI pins `MODE=release` explicitly. | Rule 1 (Makefile doc/behavior alignment surfaced by QA) |

## 3. Traceability matrix

The initiative includes migrations and refactors, so Rule 1 requires a
bidirectional matrix. The forward matrix maps each requirement to the files that
satisfy it and the tests that validate it; the backward matrix maps each
created or modified file group back to the requirement it serves. All paths are
target paths from the Agent Action Plan file-transformation mapping.

### 3.1 Forward matrix (requirement -> implementation)

| Work item | Requirement (short) | Target file(s) | Test(s) / Validation |
|-----------|---------------------|----------------|----------------------|
| P1a | Graph capture: record-once, replay-many | `src/api/graph.cpp`, `src/core/graph.hpp`, `src/core/graph.cpp`, `src/core/epoch.hpp`, `src/core/epoch.cpp`, `include/haze/haze.h` | `test/test_graph_capture.cpp`, `test/test_build.cpp` |
| P1b | Benchmark harness + regression gate; `make bench` | `benchmark/CMakeLists.txt`, `benchmark/bench_compute.cpp`, `benchmark/bench_record_replay.cpp`, `benchmark/baseline.json`, `CMakeLists.txt`, `Makefile`, `flake.nix`, `.github/workflows/benchmark.yml` | CI benchmark job compares JSON output against `benchmark/baseline.json` |
| P2a | Multi-device / peer access (simulator) | `src/api/device.cpp`, `src/api/memory.cpp`, `src/core/device.hpp`, `src/core/device.cpp`, `include/haze/haze.h` | `test/test_peer_access.cpp` (hardware-gated tag), `test/test_build.cpp` |
| P2b | Performance counters | `include/haze/haze_types.h`, `src/api/device.cpp`, `src/core/metrics.hpp`, `src/core/metrics.cpp`, `src/core/epoch.cpp`, `src/core/allocator.cpp`, `include/haze/haze.h` | `test/test_performance_counters.cpp` |
| P2c | Coverage measurement + 80% gate | **Delivered:** the `HAZE_COVERAGE` CMake option (`CMakeLists.txt`, Clang-gated), the `make coverage` target (`Makefile`, see [D-39](#d-39)), `scripts/coverage.sh` (source-based `llvm-profdata` merge + scoped `llvm-cov export -format=lcov` + 80% gate — see [D-38](#d-38)), and `.github/workflows/coverage.yml` (the per-PR CI gate). | Delivered option/target/script verified end-to-end (`make coverage` → 85.29% line coverage on `src/core`+`src/api`, gate passes); the 80% gate is logically sequenced after backfill/hardening (see [D-06](#d-06)). |
| P2d | Backfill tests for the remaining public functions | `CMakeLists.txt` (test registration) | `test/test_stream_event_lifecycle.cpp`, `test/test_async_ops.cpp`, `test/test_device_api.cpp`, `test/test_host_memory.cpp`, `test/test_introspection.cpp`, `test/test_documented_noops.cpp` |
| P2e | Error-path and edge-case hardening | `CMakeLists.txt` (test registration), the sanitizer **build options** `HAZE_SANITIZERS` (ASan/UBSan) and `HAZE_TSAN` (TSan), and a dedicated `.github/workflows/sanitizers.yml` CI job. | **Delivered:** `test/test_error_paths.cpp` (graph launch/update/destroy and allocator lock-order concurrency under `[.][concurrency]` — see [D-18](#d-18)/[D-32](#d-32)), `test/test_unflushed_reads.cpp`, `test/test_error_semantics.cpp` (fork death-test), `test/test_config_errors.cpp`, and `test/test_allocator_limits.cpp` (see [D-17](#d-17)). The `[.][concurrency]` cases were run under a local `HAZE_TSAN=ON` build with **zero** data races; the `.github/workflows/sanitizers.yml` job runs the ASan/UBSan and TSan configurations. |
| P3a | Complete CONTRIBUTING with a marked CLA placeholder | `CONTRIBUTING.md` | Markdown review; internal and external links resolve |
| P3b | `docs/` + `examples/`; keep docs-as-tests green | `docs/index.md`, `docs/architecture.md`, `docs/building.md`, `docs/testing.md`, `examples/quickstart.c`, `examples/ckks22.cpp`, `examples/README.md`, `examples/CMakeLists.txt`, `README.md`, `scripts/test_readme_examples.sh` | `scripts/test_readme_examples.sh` (docs-as-tests CI) |
| Rule 1 | Explainability: decision log + traceability | `docs/decision-log.md` | This document; the coverage assertion in [section 3.3](#33-coverage-assertion) |
| Rule 2 | Observability (library-context reinterpretation) | `src/common/log.hpp`, `src/common/log.cpp` — wired into `src/core/epoch.cpp` (flush/replay) and `src/core/graph.cpp` (launch); `docs/observability/dashboard-template.json`, `docs/observability/README.md` | Non-zero correlation IDs and `epoch.flush` / `graph.launch` spans on the real record→flush→replay path (see [D-27](#d-27)), basename-redacted paths (see [D-28](#d-28)), a `runtime_readiness()` query (see [D-29](#d-29)), and the `hazeGetPerformanceCounters` metrics surface; end-to-end evidence in `test/test_documented_noops.cpp` and `test/test_observability.cpp` |
| Rule 3 | Executive presentation | `docs/presentation/executive-summary.html` | Single self-contained reveal.js deck (one HTML file); pinned CDN assets require network and degrade to captioned fallbacks offline (see [D-13](#d-13)); Mermaid rendered under sandbox security with trusted definitions and regression guards (see [D-33](#d-33)); status/guarantee claims aligned to verified implementation (RUL3); 12–18 slides (target 16) |

### 3.2 Backward matrix (implementation -> requirement)

| File (or file group) | Work item(s) it satisfies | Notes |
|----------------------|---------------------------|-------|
| `src/api/graph.cpp` | P1a | Fill the seven graph-capture stubs; trace-signature topology gate on `hazeGraphExecUpdate` (see [D-35](#d-35)). |
| `src/core/graph.hpp`, `src/core/graph.cpp` | P1a | New `GraphState` / `GraphExec` snapshot module (see [D-03](#d-03)). |
| `src/core/epoch.hpp`, `src/core/epoch.cpp` | P1a, P2b | Trace-snapshot primitive without post-replay reset; op-count and flush-timing metric hooks. In-process re-dispatch reading current shadows (see [D-34](#d-34)); trace-signature register canonicalization (see [D-35](#d-35)); RAII vendor captured-state guard (see [D-36](#d-36)); cereal-read guards translating corrupt artifacts to `HAZE_ERROR_INTERNAL` (see [D-37](#d-37)). |
| `src/api/device.cpp` | P2a, P2b | Fill peer-access stubs and the performance-counter stub. |
| `src/api/memory.cpp` | P2a | Fill `hazeMemcpyPeerAsync`. |
| `src/core/device.hpp`, `src/core/device.cpp` | P2a | Simulator peer-topology model (see [D-09](#d-09)). |
| `src/core/metrics.hpp`, `src/core/metrics.cpp` | P2b | New counter aggregator (op counts, bytes moved, flush timings). |
| `src/core/allocator.cpp` | P2b | Bytes-moved counter hooks. |
| `include/haze/haze_types.h` | P2b | Additive `hazePerformanceCounters` struct (see [D-07](#d-07)). |
| `include/haze/haze.h` | P1a, P2a, P2b | Documentation-comment updates only; no signature change; documents the per-process program-directory requirement for concurrency (see [D-37](#d-37)). |
| `src/common/log.hpp`, `src/common/log.cpp` | Rule 2 | Structured logging with correlation IDs (see [D-04](#d-04)); line-atomic single-insertion emission (see [D-16](#d-16)). |
| `CMakeLists.txt` | P1a, P1b, P2b, P2d, P2e | Register new core sources in `haze_objs`; add the `HAZE_BUILD_BENCHMARKS`, `HAZE_SANITIZERS`, and `HAZE_TSAN` options; register all new test files, each exactly once (see [D-26](#d-26)). The `HAZE_COVERAGE` option is delivered (Clang-gated coverage instrumentation), and `.github/workflows/coverage.yml` is present in the tree (see the P2c forward row). |
| `Makefile` | P1b, P2c | Add `make bench` and coverage targets; pin the instrumentation toggles per target so ordinary builds stay clean (see [D-40](#d-40)); align the `MODE` default documentation to the actual `debug` default (see [D-41](#d-41)). |
| `flake.nix` | P1b, P2c | Add `gbenchmark` (and optional `lcov`) to the dev shell. |
| `benchmark/CMakeLists.txt`, `benchmark/bench_compute.cpp`, `benchmark/bench_record_replay.cpp`, `benchmark/baseline.json` | P1b | Harness and checked-in regression baseline (see [D-01](#d-01), [D-08](#d-08)). |
| `scripts/coverage.sh` | P2c | `llvm-profdata merge` + scoped `llvm-cov export -format=lcov` + 80% gate (see [D-02](#d-02)). |
| `scripts/test_readme_examples.sh` | P3b | Keep docs-as-tests green across README <-> `examples/` (see [D-05](#d-05)). |
| `.github/workflows/benchmark.yml` | P1b | Benchmark regression job (see [D-10](#d-10)). |
| `.github/workflows/coverage.yml` | P2c | Coverage report + 80% gate CI job — present in the tree; builds with the `HAZE_COVERAGE` option and runs the `make coverage` / `scripts/coverage.sh` flow against the 80% gate (see [D-10](#d-10), and the P2c forward row). |
| `.github/workflows/sanitizers.yml` | P2e | ASan/UBSan + TSan CI job — present in the tree; exercises the `HAZE_SANITIZERS`/`HAZE_TSAN` build options (the `[.][concurrency]` cases ran locally with zero races) (see [D-10](#d-10)). |
| `test/test_build.cpp` | P1a, P2a | Update the graph and peer `NOT_SUPPORTED` assertions; redirect the last-error helper (ripple). |
| `test/test_graph_capture.cpp` | P1a | Capture / instantiate / launch / update / destroy and replay-determinism tests; QA-remediation coverage for record-once / replay-many re-dispatch (see [D-34](#d-34)), topology-change rejection (see [D-35](#d-35)), and corrupt-artifact ABI safety (see [D-37](#d-37)). |
| `test/test_peer_access.cpp` | P2a | Peer tests behind a hardware-gated tag. |
| `test/test_performance_counters.cpp` | P2b | Assert non-zero counters after a workload. |
| `test/test_stream_event_lifecycle.cpp`, `test/test_async_ops.cpp`, `test/test_device_api.cpp`, `test/test_host_memory.cpp`, `test/test_introspection.cpp`, `test/test_documented_noops.cpp` | P2d | Backfill coverage for the remaining public functions and documented no-op smoke tests. |
| `test/test_error_paths.cpp`, `test/test_unflushed_reads.cpp`, `test/test_error_semantics.cpp`, `test/test_config_errors.cpp` | P2e | Negative and boundary tests across eight of the nine hardening categories; `test_error_paths.cpp` also carries the epoch/allocator lock-order concurrency test behind the hidden `[.][concurrency]` tag (see [D-18](#d-18)). |
| `test/test_allocator_limits.cpp` | P2e | Allocator pool-exhaustion / zero / oversized boundary tests — **delivered**, registered exactly once in `add_executable(haze_tests ...)`, and running in the default suite (see [D-17](#d-17)). |
| `test/test_observability.cpp` | Rule 2 | Functional correlation-ID and span-tracing evidence across the record -> flush -> replay path: enabled-span begin/end stamping, thread-local IDs, the `HAZE_TRACE` toggle, and per-epoch non-zero IDs on a real `hazeFlush` (complements the no-op telemetry evidence in `test/test_documented_noops.cpp`). |
| `CONTRIBUTING.md` | P3a | Full contributor guidelines with a clearly marked CLA placeholder. |
| `README.md` | P3b | Add graph/peer/counter status, benchmarking, coverage, and observability sections; link `docs/` and `examples/`. |
| `docs/index.md`, `docs/architecture.md`, `docs/building.md`, `docs/testing.md` | P3b | New documentation tree. |
| `examples/quickstart.c`, `examples/ckks22.cpp`, `examples/README.md`, `examples/CMakeLists.txt` | P3b | Migrated runnable examples mirrored from the README markers. |
| `docs/decision-log.md` | Rule 1 | This file — the single source of truth for rationale. |
| `docs/observability/dashboard-template.json`, `docs/observability/README.md` | Rule 2 | Dashboard template and the counters -> metrics / health / tracing mapping. |
| `docs/presentation/executive-summary.html` | Rule 3 | Self-contained reveal.js executive deck (see [D-12](#d-12)); Mermaid sandbox-hardened with trusted definitions and regression guards (see [D-33](#d-33), fixes S1); status/guarantee claims aligned to verified implementation (fixes RUL3). |
| `src/api/stream.cpp` (`hazeStreamSynchronize`, `hazeStreamWaitEvent`); `src/api/device.cpp` (`hazeDeviceSynchronize`) | Out of scope | The three documented no-ops are correct-by-design and intentionally NOT modified; behavior is unchanged and only smoke-tested (in `test/test_documented_noops.cpp`). |

### 3.3 Coverage assertion

**Scope of this assertion.** The forward and backward matrices above cover
every Agent Action Plan work item (P1a, P1b, P2a, P2b, P2c, P2d, P2e, P3a, P3b)
and every rule (Rules 1–3), and — with the single-pass integrated delivery (see
[D-22](#d-22)) — they now describe **delivered, verified artifacts against the
current tree**, not a plan-level intent. Each row states delivered-vs-deferred
explicitly so no absent surface is silently credited.

**Delivered and verified in the tree:** the graph/peer/counter **runtime**
(`src/core/graph.{hpp,cpp}`, `src/core/metrics.{hpp,cpp}`, the epoch snapshot
primitive, the peer-topology model, and the counter hooks) with real
record-once/replay-many, topology-checked exec update, and populated counters;
the **benchmark harness** (`benchmark/*`, the `make bench` target, the
`HAZE_BUILD_BENCHMARKS` option, `.github/workflows/benchmark.yml`, and a
genuinely-measured `benchmark/baseline.json` — see [D-30](#d-30)); the
**observability** wiring (non-zero correlation IDs, `epoch.flush`/`graph.launch`
spans, basename redaction, `runtime_readiness()`); the **coverage stack** (the
`HAZE_COVERAGE` CMake option, the `make coverage` target, `scripts/coverage.sh`,
and the `.github/workflows/coverage.yml` per-PR CI gate
— instrument -> run -> merge -> scoped lcov export -> 80% gate, measured at 85.29%
line coverage on `src/core`+`src/api`, see [D-38](#d-38)); and all fourteen new
`test/*.cpp` translation units (the thirteen backfill/hardening TUs plus
`test/test_allocator_limits.cpp`), each registered exactly once and passing.
Every non-hidden case passes and the `[.][concurrency]` cases run race-free
under `HAZE_TSAN=ON`; the `.github/workflows/sanitizers.yml` job runs the
ASan/UBSan and TSan configurations in CI.

**Genuinely deferred (marked as such in the matrices, not credited):**
physical multi-chip peer-access validation (human follow-up, see
[D-09](#d-09)), which requires hardware absent from CI.

**Intentional exclusions.** The three documented no-ops
(`hazeStreamSynchronize`, `hazeStreamWaitEvent`, `hazeDeviceSynchronize`) are the
only intentionally excluded *functions* and are recorded above as out-of-scope,
smoke-tested only (see [D-19](#d-19)).

### 3.4 Delivered per-test-case coverage

This section records the requirement/test-case granularity R3 requires for the
delivered translation units. Each row names the specific semantic requirement
each file exercises so coverage can be audited at the test-case level rather
than only at the file level. Every listed file runs against the delivered
runtime and passes.

| Delivered test file | Semantic requirements exercised (per-case intent) |
|---------------------|---------------------------------------------------|
| `test/test_graph_capture.cpp` | Begin/end-capture state machine (nested-begin rejected `INVALID_VALUE`; end-without-begin `INVALID_VALUE` distinct from empty-capture `SOURCE_UNAVAILABLE`); instantiate/launch/update/destroy lifetime; same-topology update relaunches and changes results; genuine topology-mismatch rejection; exhaustive per-arg null/invalid/destroyed-handle validation with output-zeroing, last-error, and state recovery. Asserts the **final implemented** contract (runtime delivered and verified). |
| `test/test_peer_access.cpp` | query -> enable -> authorization contract; disabled/enabled states; negative and out-of-range device/copy ordinals; simulator peer copy (tag/materialize/D2H/byte-compare); hardware readback behind the hidden `[.]` tag. Asserts the **final implemented** contract (runtime delivered and verified). |
| `test/test_performance_counters.cpp` | Exact before/after deltas per op (not `>=`); one-flush `flush_count==1` and `total==last`; two-flush exact totals; `offsetof` layout assertions for all seven fields; dirty-then-reset; failed-op no-delta; `hazeWriteProgram` no-flush. Asserts the **final implemented** contract (runtime delivered and verified). |
| `test/test_stream_event_lifecycle.cpp` | Nonzero-flag contract; direct null-output rejection for `*WithPriority`/`*WithFlags`; null-handle destroy/record. |
| `test/test_async_ops.cpp` | Default-stream malloc/free/memset; invalid source/kind/size paths; allocator-only `[unit]` retag. |
| `test/test_device_api.cpp` | `GetDeviceCount`/`SetDevice`/`GetDevice`/`GetDeviceProperties`; negative-device properties output + last-error. |
| `test/test_host_memory.cpp` | `HostAlloc`/`FreeHost` acceptance contract; safe negative paths; post-free reclassification. |
| `test/test_introspection.cpp` | `PointerGetAttributes` exact fields/device association; null and post-free classification; MRP device-classification. |
| `test/test_documented_noops.cpp` | The three no-ops return `HAZE_SUCCESS` without behavior change; **executable observability evidence** — all seven counters and the correlation ID are snapshotted before/after the no-ops and asserted unchanged, and a composed log line is asserted single-line and escaped (Rule 2, see [D-16](#d-16)). |
| `test/test_error_paths.cpp` | Null/invalid handles, use-after-free, double-free, wrong allocator with correctly-sized buffers and a sentinel-unchanged assertion; the epoch/allocator lock-order concurrency test under `[.][concurrency]` (see [D-18](#d-18)). |
| `test/test_unflushed_reads.cpp` | `NOT_FLUSHED` before flush; correct readback after tag+flush; device-vs-shadow pointer classification. |
| `test/test_error_semantics.cpp` | Explicit 18-entry `{internal, expected_public}` mapping asserted for exact equality; a fork-subprocess death-test proving no C++ exception crosses the `noexcept` C ABI boundary (see [D-20](#d-20)). |
| `test/test_config_errors.cpp` | The **actual** sparse-write/contiguity + post-configure-immutability contract of `Config::set_modulus` (see [D-21](#d-21)); ring-dim/device configuration errors. |
| `test/test_allocator_limits.cpp` | Device-allocator exact-polynomial-size validation: `hazeMalloc` before the ring dimension is configured returns `CONFIGERR`; zero / undersized / oversized requests return `ALLOC_TOO_SMALL`; the exact `ring_dim * sizeof(uint64_t)` size is accepted and yields a device address at or above `kHbmBase`; a large batch of allocations is all-distinct; and freed blocks are recycled from the free-list. The ninth P2e hardening category — allocator limits (see [D-17](#d-17)). |
| `test/test_observability.cpp` | Enabled `TraceSpan` emits exactly one `trace.span.begin` / `trace.span.end.ok` record per edge, each stamped with the current correlation ID; `HAZE_TRACE` toggles `trace_enabled()` (re-read each call); tracing is off by default (a span emits nothing); the correlation ID is thread-local; a real `hazeFlush` emits `epoch.flush` begin/end spans carrying one non-zero, per-epoch-unique ID; and a tracing-off cycle emits no trace records. Asserts the **final implemented** contract (runtime delivered and verified). |


## 4. Deviations and assumptions

### 4.1 Deviations

Each deviation from a literal reading of the requirements is logged here and
linked to its decision-table entry, per Rule 1.

- **Single integrated branch supersedes the per-work-item PR directive.** The
  brief calls for "separate PRs per work item (or per priority tier)"; the
  remediation instead lands the entire initiative — runtime, tests, benchmarks,
  observability, and docs — as one coherent branch so every review finding is
  resolved and verified together against a single tree. Consequently, artifacts
  the earlier framing described as "deferred to a later PR" (the graph/peer/counter
  runtime, `test/test_allocator_limits.cpp`) are delivered now, and only physical
  multi-chip peer-access validation remains marked deferred. See [D-22](#d-22).
- **Observability reinterpreted for a library context.** The rule's
  network-service vocabulary (metrics endpoint, distributed tracing, health
  checks) is mapped to library equivalents rather than implemented literally.
  See [D-04](#d-04).
- **Executive summary embeds its theme inline.** The canonical
  `blitzy-deck/references/blitzy-reveal-theme.css` is absent from the checkout,
  so the deck is delivered as a single self-contained HTML file with the theme
  embedded; the canonical file's contents are not fabricated. See [D-12](#d-12).
- **Coverage gate deferred behind backfill/hardening.** Rather than enabling the
  80% gate immediately, it is sequenced after P2d/P2e raise coverage — a
  deviation from a literal "add the gate now" reading, justified as a logical
  dependency. See [D-06](#d-06).
- **The C1 finding's literal "revert the registrations" suggestion is declined
  on AAP-precedence grounds.** C1 suggests moving the `src/core/{graph,metrics}.cpp`
  and new-test registrations out of this change into a separate final-integration
  pair. AAP §0.6.1 lists `CMakeLists.txt` as an in-scope UPDATE that must "register
  all new test files," and §0.6.5 states every new `test/*.cpp` must be added to
  `add_executable(haze_tests ...)` "or it will not be compiled or run." Because the
  AAP overrides a finding's suggested resolution, the registrations are kept — each
  source registered exactly once (verified) — and the actionable half of C1
  (exactly-once, no duplicate registration) is satisfied. See [D-26](#d-26).
- **The benchmark regression check is advisory rather than a hard CI gate.** A
  literal reading of P1b ("flag regressions against a baseline") could imply a
  fail-the-build gate, but CI shares runners with variable performance, so the
  comparison is scoped as advisory with a tolerance band and recorded host
  metadata to avoid failing on noise. See [D-30](#d-30).
- **`examples/` is kept outside the `clang-format` enforcement scope.** A literal
  "keep clang-format clean" reading would add `examples/` to the formatted set,
  but the example sources are generated mirrors of the authoritative README fenced
  blocks and must match the README's presentation formatting and the docs-as-tests
  extraction; enforcing repository `clang-format` on them would fight that. They
  are instead kept format-idempotent by construction and proven equivalent to the
  README by `scripts/test_readme_examples.sh`. See [D-31](#d-31).
- **"Opens offline" claim corrected; CDN load made resilient.** The traceability
  matrix previously described the deck as opening offline, but it references
  pinned CDN assets (reveal.js, Lucide, Mermaid) and therefore requires network
  for full fidelity. The claim is corrected to state the deck is a single
  self-contained file that requires network for its pinned CDN assets, and
  reveal.js initialization is decoupled from the Mermaid CDN so a diagram-CDN
  outage degrades to captioned fallbacks rather than blanking the deck. See
  [D-13](#d-13).
- **Diagnostic lines are composed then emitted with one `std::fwrite`.** Rather
  than performing several independent per-field writes (which would interleave
  under concurrent multi-threaded logging), each record is composed into a
  single `std::string` and written with one `std::fwrite(..., stderr)` to
  satisfy the line-integrity / concurrency-isolation property of Rule 2. Field
  contents are neutralized deterministically by `append_escaped` (newlines,
  control bytes, and non-ASCII rendered as `\xNN`), so framing integrity does
  not depend on a timing-sensitive thread race. On any formatting/write failure
  the `catch (...)` handler emits a fixed, allocation-free generic dropped-record
  notice via `std::fputs` — not a re-emission of the record — preserving
  `noexcept`. See [D-16](#d-16).
- **Peer access is simulator-only and validates device ordinals.** Physical
  multi-chip peer transfer needs hardware absent from CI, so only the
  simulator-representable query -> enable -> copy path is implemented, and the
  peer entry points validate device ordinals (rejecting negative and
  out-of-range indices) rather than accepting them unchecked as the superseded
  comment implied. Hardware-only assertions sit behind a hidden tag and the
  multi-chip step is flagged as human follow-up. See [D-09](#d-09).
- **`test/test_config_errors.cpp` exercises the real `set_modulus` contract, not a
  nonexistent index cap.** A literal reading of the review suggested asserting
  that index 64 is rejected after populating 0–63; an empirical probe proved
  `Config::set_modulus` has no hard 64-index cap (writing 64 and 65 succeeds
  after a contiguous fill), so the test exercises the actual sparse-write /
  contiguity / post-configure-immutability contract instead. See [D-21](#d-21).
- **`test/test_allocator_limits.cpp` is delivered.** With the single-pass
  integrated delivery (see [D-22](#d-22)) there is no separate later PR to defer
  to, so the allocator pool-exhaustion / zero / oversized boundary TU is present
  in the tree, registered exactly once in `add_executable(haze_tests ...)`, and
  runs in the default suite. The earlier "deferred" rationale is withdrawn because
  it is no longer true against the checkout. See [D-17](#d-17).
- **The lock-order concurrency test is scoped to independent per-thread
  allocation churn.** A literal "epoch/allocator lock-order under concurrent op
  recording" test is not expressible because record-and-replay is single-writer,
  so the delivered `[.][concurrency]` test in `test/test_error_paths.cpp`
  exercises the allocator lock under eight-thread contention while the nested
  epoch-path ordering is documented as deferred. See [D-18](#d-18).
- **Concurrency stress is co-located with each surface it defends.** Beyond the
  allocator lock-order case, the delivered `[.][concurrency]` suite now covers
  peer-call races (`test/test_peer_access.cpp`), graph launch/update/destroy
  lifetime races (`test/test_error_paths.cpp`), and counter snapshot/reset races
  (`test/test_performance_counters.cpp`); all were run under `HAZE_TSAN=ON` with
  zero data races. The counter case is the single documented exception that
  reaches the internal `haze::metrics()` aggregator, because a public
  `hazeDeviceReset` tears down the allocator and cannot safely race active device
  work. See [D-32](#d-32).
- **The no-exception-across-the-ABI proof uses a fork death-test.** Because a
  throw escaping a `noexcept` function terminates the process before any
  in-process matcher can observe it, `REQUIRE_NOTHROW` cannot prove the property;
  a fork-subprocess death-test is used instead. See [D-20](#d-20).
- **White-box test access is used only where a public getter does not exist.**
  The public C ABI intentionally exposes no state getters, so a few tests reach
  internal `haze::` symbols (for example `haze::to_public_error` for the ES2
  mapping table, and the `AllocatorTestAccess` helper already used by the
  baseline `test/test_memory.cpp` for MRP-allocation negatives). This is a
  deliberate white-box choice for assertions that have no black-box surface;
  MRP-allocation negative cases remain owned by `test/test_memory.cpp` and are
  not duplicated in the new introspection tests.


### 4.2 Assumptions

- **Toolchain availability.** Google Benchmark 1.9.5 resolves from the pinned
  nixpkgs as `gbenchmark`, and `llvm-cov` / `llvm-profdata` are provided by the
  Clang 19 toolchain with no additional runtime dependency. See [D-01](#d-01),
  [D-02](#d-02).
- **Integrated tree (assumption discharged).** The files referenced here — for
  example `../src/core/graph.hpp` and `./observability/README.md` — are all
  present in the integrated single-pass branch (see [D-22](#d-22)), so the
  relative links in this log resolve against the actual tree rather than assuming
  future sibling PRs. This assumption is therefore discharged, not pending.
- **Pointer stability.** `DevAddr` operands remain stable across graph replays;
  this is the correctness invariant underpinning graph capture. See
  [D-03](#d-03).
- **Concurrent-process isolation.** Concurrent processes that record and replay
  must each set a distinct `hazeSetProgramDirectory` before their first compute
  call; the shared base recording directory is otherwise a cross-process
  data-mixing hazard, and corrupt or partial on-disk artifacts always surface as
  `HAZE_ERROR_INTERNAL` rather than a C++ exception crossing the `noexcept` C
  ABI. See [D-37](#d-37).
- **Export contract.** The `haze*` version-script wildcard remains the sole
  export contract, so filling the already-exported stubs needs no change to
  `linker/haze_exports.{map,sym}`. See [D-11](#d-11).
